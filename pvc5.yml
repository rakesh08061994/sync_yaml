apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc05
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: slow

