apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc02
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 4Gi
  storageClassName: slow

